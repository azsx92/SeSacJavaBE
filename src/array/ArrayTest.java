package array;

public class ArrayTest {
    public ArrayTest() {
        int[] a = new int[3];
        for (int i = 0; i < a.length ; i++) {
            System.out.println(a[i] =i+3);
        }


     /*
        for (int i = 0; i < a.length ; i++) {
            a[i] =i+3;
            System.out.println(a[i]);
        }
     */
    }
}
